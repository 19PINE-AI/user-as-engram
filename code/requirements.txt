# Python dependencies for the experiment + baseline scripts under code/.
#
# torch and the tokenizer come from the vendored harness
# (code/nanochat_harness/, installed with `pip install -e code/nanochat_harness`).
# The packages below are what our scripts in code/scripts/ and code/lora_baseline/
# pull in on top of it. Install into the same environment:
#
#   pip install -e code/nanochat_harness
#   pip install -r code/requirements.txt
#
# (torch is intentionally unpinned so it stays consistent with the harness.)

numpy>=1.24
matplotlib>=3.7

# Retrieval / instruction-tuned baselines (Mem0/MemMachine-style retrieval,
# Qwen-3B + RAG, cross-base LoRA). Used by code/scripts/memory_systems_*.py,
# eval_at_scale.py, cross_lm_transfer.py, judge_locomo.py, multifact_lora.py, sft_*.py.
transformers>=4.40
sentence-transformers>=2.6

# The per-user-LoRA (POLAR) baseline and trace pipeline in code/lora_baseline/.
peft>=0.10
requests>=2.31

# Exact arithmetic in a couple of analyses.
sympy>=1.12
